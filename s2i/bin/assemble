#!/bin/bash -e
#
# S2I assemble script for the 'centos/python-35-centos7' image.
# The 'assemble' script builds your application source so that it is ready to run.
#
# For more information refer to the documentation:
#	https://github.com/openshift/source-to-image/blob/master/docs/builder_image.md
#

# If the 'centos/python-35-centos7' assemble script is executed with the '-h' flag, print the usage.
if [[ "$1" == "-h" ]]; then
	exec /usr/libexec/s2i/usage
fi

# Restore artifacts from the previous build (if they exist).
#
if [ "$(ls /tmp/artifacts/ 2>/dev/null)" ]; then
  echo "---> Restoring build artifacts..."
  mv /tmp/artifacts/. ./
fi

echo "---> Installing application source..."
cp -Rf /tmp/src/. ./

echo "---> Building application from source..."
# TODO: Add build steps for your application, eg npm install, bundle install, pip install, etc.
set -x
mkdir -p /opt/app-root/src/.sopel
chmod +x /opt/app-root/start.sh 
mv /opt/app-root/modules/ /opt/app-root/src/.sopel/modules
mv /opt/app-root/*.tpl /opt/app-root/src/.sopel
mv /opt/app-root/malwaredomains.txt /opt/app-root/src/.sopel
